"# 0.6: New Note SPA Diagram" 

```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>browser:  JavaScript prevents default form submission
    browser->>browser: Note is added to list and UI is updated no page reload

    browser->>server: POST /new_note_spa with note data
    server-->>browser: 201 Created


